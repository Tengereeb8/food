import { AppSidebar } from "./components/side";

const Home = async () => {
  return (
    <div className="w-screen h-screen bg-white font-inter">
      <AppSidebar />
    </div>
  );
};

export default Home;
