const Home = () => {
  // const categories = await fetch("http://localhost:3002/api/categories");
  // const data = await categories.json();
  // console.log(data);

  return (
    <div className="flex items-center justify-center bg-white w-screen"></div>
  );
};

export default Home;
